[package]
name = "ntoff-extract"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "Extracts curated kernel type layouts and global symbol RVAs from a PDB"

[dependencies]
pdb = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
